<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:BenchmarkTools.Core.ViewModels;assembly=BenchmarkTools.Core"
             x:DataType="vm:DashboardViewModel"
             x:Class="BenchmarkTools.Views.DashboardPage"
             Title="{Binding Title}">
    <VerticalStackLayout Padding="20" Spacing="16">
        <Label Text="Dashboard" Style="{StaticResource Headline}" />
        <Label AutomationId="DashboardClicksLabel"
               Text="{Binding TotalClicks, StringFormat='Total Clicks: {0}'}" />
        <Label Text="{Binding LastActivity}" />
        <Button AutomationId="DashboardRefreshButton"
                Text="Refresh"
                Command="{Binding RefreshCommand}" />
        <ActivityIndicator IsRunning="{Binding IsBusy}"
                           IsVisible="{Binding IsBusy}" />
    </VerticalStackLayout>
</ContentPage>
